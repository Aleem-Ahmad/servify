# ═══════════════════════════════════════════════════════════════════
# SERVIFY — Environment variables (Supabase is the database — final)
# Copy to .env.local for local dev. Set the same keys on Vercel.
# ═══════════════════════════════════════════════════════════════════

# DATABASE — Supabase PostgreSQL via Prisma (project: stvjosvtexdiuqgmikhl)
DATABASE_URL=postgresql://postgres.stvjosvtexdiuqgmikhl:ZM.A2kr%255wg7Lqy@aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true
DIRECT_URL=postgresql://postgres.stvjosvtexdiuqgmikhl:ZM.A2kr%255wg7Lqy@aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres

# SUPABASE — project URL (optional until Supabase JS client is added; Prisma uses DATABASE_URL above)
NEXT_PUBLIC_SUPABASE_URL=https://stvjosvtexdiuqgmikhl.supabase.co

# EMAIL — Resend (required for signup OTP)
RESEND_API_KEY=
RESEND_FROM_EMAIL=noreply@servify.space

# APP
NEXT_PUBLIC_APP_URL=http://localhost:3000

# CLOUDINARY — add when image uploads are enabled (not required yet)
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=
# NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
